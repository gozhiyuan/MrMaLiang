# Small API-evaluation pilot

This reduced-scale survey validation blueprint uses at most 300 calls, three
repetitions, a fixed heldout accuracy metric, and explicit provider/model,
prompt-hash, response-hash, retry, seed, and token provenance. Configure a
credentialed transport outside the workspace; this example contains none.
