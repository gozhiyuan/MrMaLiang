# Expected Artifacts

The runner must emit per-trial normalized records. LongExperiment derives the
checksummed suite manifest and audit only after all required trial, revision,
and artifact checks pass. Configure the candidate worktree before preflight.
