[project]
name = "maliang-modal-adapter"
version = "0.1.0"
description = "Workspace-owned Modal Sandbox adapter for LongExperiment"
requires-python = ">=3.12,<3.13"
dependencies = [
  "modal>=1.0.0",
  "PyYAML>=6.0",
]

[tool.uv]
package = false
